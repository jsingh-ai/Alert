{
  "name": "processguard-teams-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone Microsoft Teams notification and acknowledgment bridge for ProcessGuard.",
  "scripts": {
    "start": "node src/index.mjs",
    "check": "node --check src/index.mjs && node --check src/server.mjs && node --check src/monitor.mjs",
    "test": "node --test --test-isolation=none"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
